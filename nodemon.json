{
    "ignore": ["**/node_modules/**", "**/.git/**", "**/target/**"],
    "watch": [
        "_scratch/**/*",
        "crates/**/*",
        "examples/**/*"
    ],
    "ext": "*",
    "delay": 250,
    "signal": "SIGTERM",
    "exec": "clear && cargo build --release --package ion_scratch && .\\target\\release\\ion_scratch.exe"
}
